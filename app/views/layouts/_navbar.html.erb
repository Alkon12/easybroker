<nav class="bg-white border-b border-gray-200" data-controller="mobile-menu">
  <div class="container mx-auto px-4 max-w-7xl">
    <div class="flex items-center justify-between h-16">
      <div class="flex items-center space-x-8">
        <%= link_to root_path, class: "flex items-center space-x-2" do %>
          <div class="w-8 h-8 bg-gradient-to-br from-ios-blue to-ios-indigo rounded-lg flex items-center justify-center">
            <span class="text-white font-bold text-lg">EB</span>
          </div>
          <span class="text-xl font-semibold text-gray-900 hidden sm:inline">EasyBroker</span>
        <% end %>

        <div class="hidden md:flex items-center space-x-1">
          <%= link_to properties_path, class: "px-4 py-2 rounded-ios text-sm font-medium transition-colors #{current_page?(properties_path) ? 'bg-ios-gray-1 text-ios-blue' : 'text-gray-600 hover:text-gray-900 hover:bg-gray-50'}" do %>
            Properties
          <% end %>

          <%= link_to locations_path, class: "px-4 py-2 rounded-ios text-sm font-medium transition-colors #{current_page?(locations_path) ? 'bg-ios-gray-1 text-ios-blue' : 'text-gray-600 hover:text-gray-900 hover:bg-gray-50'}" do %>
            Locations
          <% end %>
        </div>
      </div>

      <div class="flex items-center space-x-4">
        <span class="text-xs sm:text-sm text-gray-500 hidden sm:inline">Staging Environment</span>

        <!-- Mobile menu button -->
        <button data-action="click->mobile-menu#toggle" class="md:hidden p-2 rounded-ios text-gray-600 hover:text-gray-900 hover:bg-gray-50 transition-colors">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path data-mobile-menu-target="menuIcon" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            <path data-mobile-menu-target="closeIcon" class="hidden" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
      </div>
    </div>

    <!-- Mobile menu -->
    <div data-mobile-menu-target="menu" class="hidden md:hidden pb-4">
      <%= link_to properties_path, class: "block px-4 py-3 rounded-ios text-sm font-medium transition-colors #{current_page?(properties_path) ? 'bg-ios-gray-1 text-ios-blue' : 'text-gray-600 hover:text-gray-900 hover:bg-gray-50'}" do %>
        Properties
      <% end %>

      <%= link_to locations_path, class: "block px-4 py-3 rounded-ios text-sm font-medium transition-colors #{current_page?(locations_path) ? 'bg-ios-gray-1 text-ios-blue' : 'text-gray-600 hover:text-gray-900 hover:bg-gray-50'}" do %>
        Locations
      <% end %>
    </div>
  </div>
</nav>
